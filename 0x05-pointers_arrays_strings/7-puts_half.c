#include <stdio.h>
#include "main.h"

/**
* puts_half - prints a string
* @str: string to print
*
* Return: Always 0
*/

void puts_half(char *str)
{
	int len = 0;

	while (*(s + len) != '\0')
		len++;
	return (len);
}

